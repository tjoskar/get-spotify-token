# Get Spotify Access Token

Just a simple script to get a access and refresh token.

```
$ deno run --allow-net get-spotify-token.ts my-client-id my-client-secret
```

Visit `http://localhost:8080/get-token`
